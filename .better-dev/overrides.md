<!-- BEGIN better-dev overrides -->
- integration branch = staging (promotes to master on release)
- feature branch prefix = feat/ (fix/ for fixes)
- default/release branch = master (not main)
- auto-merge on green: merge a PR as soon as its checks pass - no per-PR human confirmation needed (operator-approved 2026-07-08)
<!-- END better-dev overrides -->
